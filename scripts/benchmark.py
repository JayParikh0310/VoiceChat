# Standalone per-component latency benchmark.
# Measures STT, LLM first-token, TTS first-chunk in isolation with test inputs.
# Run before integration to know your latency budget breakdown.
# Implementation: Part 1 (STT benchmark) → extended per Part
