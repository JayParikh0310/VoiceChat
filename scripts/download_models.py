# Download all required model files in one shot:
#   - faster-whisper base (int8)
#   - all-MiniLM-L6-v2 embeddings
#   - Kokoro-82M TTS weights
#   - Ollama model pull (calls ollama CLI)
# Run once. After this, zero network calls at runtime.
# Implementation: Part 0 / setup step
