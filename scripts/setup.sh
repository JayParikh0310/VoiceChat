#!/usr/bin/env bash
# One-shot environment setup: creates venv, installs requirements, pulls Ollama model.
# Run once before starting development.
# Implementation: Part 0 / setup step (not counted as a code part)
