# Latency Benchmark Report

## Hardware
- CPU: Intel Core i9-13900HX (13th Gen)
- GPU: NVIDIA GeForce RTX 4060 Laptop (8GB VRAM), driver 596.08, CUDA 13.2
- RAM: 31.7 GB

## Per-Component Benchmarks (median over 20 runs)

| Component | Median Latency | P95 Latency | Notes |
|---|---|---|---|
| VAD | not yet benchmarked | — | Correctness-only in Part 1 (`tests/test_vad.py`, 8/8 passing). Silero VAD is a ~2MB model; expected negligible (<5ms/window) per `notes.md`, but no numeric benchmark exists yet — `scripts/benchmark.py` only covers STT/TTS/full-pipeline per its `--component` choices. Add a VAD section there if a hard number is needed for submission. |
| STT (faster-whisper **small**, int8, CUDA) | **68 ms** | **81 ms** | 5s synthetic test clip, `scripts/benchmark.py --component stt --runs 20`. Min/max: 58ms/81ms. Well under the 400ms budget slice. Note: config uses `small` (not `base` as originally planned in `notes.md`) since the RTX 4060's 8GB VRAM comfortably affords it — see decision note below. |
| LLM first token (qwen2.5:3b via Ollama, warm) | **587 ms** | — | Single hardcoded-prompt run via `agent/graph.py`, `stream_mode="custom"`. Cold (model not yet loaded into VRAM): **~36s** — confirms the "pre-warm at startup" decision in `tradeoffs.md` is load-bearing, not optional; Part 7 must fire a dummy prompt before the pipeline accepts real input. |
| retrieve_memory (embed query + FAISS search, on critical path) | **25 ms** | — | 10 runs, 3-fact store, `memory/long_term.py`'s `retrieve_similar()`. Negligible against the 2s budget — runs before `generate`, so it adds to (not overlaps with) the 587ms LLM number above. |
| extract_memory (classify + store, off critical path) | not on critical path | — | Fire-and-forget `asyncio.create_task` — the graph node itself returns near-instantly; the actual Ollama classification call (~0.5-1s) and store happen in the background and don't block this turn's audio output. |
| TTS per-sentence synthesis (Kokoro-82M, CUDA) | **~120 ms** | — | Warm, 3 test sentences (13-16 words each): 167ms/109ms/98ms. Measured via `tests/test_tts.py::test_synthesize_latency_within_budget` (5 runs, asserts <300ms). Requires CUDA torch — see note below; on CPU-only torch this was 1200-1500ms/sentence, ~10x slower. |
| Input guardrail (`self_check_input`, qwen2.5:3b via Ollama) | **328 ms** | — | 10 runs, warm, `guardrails/manager.py`'s `check_input()`. One extra Ollama call, sequenced before `generate_node` starts — adds directly to first-token latency (not overlapped). |
| Output guardrail (`self_check_output`, qwen2.5:3b via Ollama) | **324 ms** | — | 10 runs, warm, `check_output()`. Applied per sentence chunk once wired into the pipeline (Part 7) — on a 3-sentence response this is ~3×324ms ≈ 1s of *additional* sequential LLM time across the turn, since each sentence's TTS is gated on its own check passing first. See Analysis below. |
| Fallback filler-phrase overhead (synthesis, beyond the 800ms timeout itself) | **~73-113 ms** | — | 5 runs, warm, `pipeline/fallback.py`'s `FallbackManager.watch()` timed with a fake audio manager (isolates synthesis from real playback time). This is time *added on top of* the 800ms timeout before the filler phrase is actually audible — see Analysis below. |
| **End-to-end total** | **pending Part 7** | — | **mic → first audio out** |

## Analysis

STT, LLM/memory, TTS, and guardrails are all benchmarked now (Parts 1-5). 68ms STT + 25ms retrieve + 328ms input guardrail + 587ms LLM first token + ~120ms first-sentence TTS ≈ **~1130ms** to first audio actually leaving the speaker, warm, *if* the output guardrail runs in parallel with something else or is budgeted separately (see below) — well inside the 2s budget, before VAD or full pipeline overhead (Part 7) are added.

**The 800ms fallback threshold is close to the median warm LLM first-token latency (587ms), and firing it isn't free.** The fallback is meant to catch a slow turn and keep the assistant feeling responsive — but `watch()`'s own timeout (800ms) plus the filler phrase's own synthesis time (~73-113ms warm, measured above) means the filler phrase isn't actually audible until roughly **875-915ms** into a slow turn, not 800ms. That's already later than a *normal* warm turn's LLM first-token latency (587ms) would have delivered the real response. In other words: on this hardware, in the warm/no-guardrails case, the fallback should rarely fire at all (587ms < 800ms comfortably) — but the moment input guardrails are wired in ahead of `generate_node` (Part 7), the input check's own ~328ms pushes the effective time-to-first-real-token to ~915ms (328 + 587), which *exceeds* the 800ms fallback threshold on nearly every turn. Left as-is, that means the fallback would fire on almost every guarded turn — worth revisiting the threshold value, or where exactly the 800ms timer starts relative to the guardrail check, once Part 7 wires both pieces into the same turn. Noting this now while it's cheap to fix, rather than after full pipeline integration.

**Guardrails add real, non-overlapped latency — this is a genuine budget cost, not a rounding error.** The input check (328ms) sits squarely on the critical path: it must finish before `generate_node` can even start, since its whole job is deciding whether to let the transcript reach the LLM at all. The output check (324ms) is worse in aggregate: per the plan, it's meant to run per *sentence chunk*, not once per full response, so a typical 2-3 sentence reply pays 324ms × (number of sentences) in additional sequential Ollama calls — on top of, not overlapped with, that sentence's TTS synthesis (~120ms). For a 3-sentence response, that's roughly +1s of guardrail overhead alone, on top of the ~800ms baseline from Part 4. This pushes a "just barely fits" turn into "over budget" territory once Part 7 wires guardrails into the real pipeline, and is worth watching closely once end-to-end numbers exist. Two mitigations to consider at Part 7: (1) run `check_output` concurrently with starting that sentence's TTS synthesis and only gate the *playback* on the check result (accepts wasted synthesis work on a block, but overlaps the two ~300ms costs instead of stacking them), or (2) check only the first sentence per turn and let the rest through, trading some safety coverage for latency. Neither is implemented yet — Part 5 only builds and tests the guardrail checks in isolation, per the plan's explicit split ("wired at pipeline level," Part 7).

**Environment note — Windows CUDA runtime gap (recurring theme)**: this is the *second* time in this project a default `pip`/`uv` install silently gave a CPU-only build on Windows where a GPU one was expected — same root cause class as the STT cuBLAS/cuDNN issue in Part 1.
- STT (Part 1): `faster-whisper`/CTranslate2 needed `nvidia-cublas-cu12`/`nvidia-cudnn-cu12` — fixed by prepending their DLL dirs to `PATH` in `audio/stt.py`.
- TTS (Part 4): `torch` itself installs CPU-only from the default PyPI index on Windows (unlike Linux, where the default wheel bundles CUDA) — this silently made Kokoro synthesis ~10x slower without erroring. Fixed in `pyproject.toml` via `[tool.uv.sources]`/`[[tool.uv.index]]`, scoping just `torch`/`torchaudio` to PyTorch's `cu126` index (exact version pin `==2.6.0` on both — a mismatched `torchaudio` version against a pinned `torch` build crashes on import with `OSError: [WinError 127]`, a second bug found while fixing the first).
- **Takeaway for later parts**: if anything CPU-bound and unexpectedly slow shows up again, check `torch.cuda.is_available()` and the exact package/DLL search path before assuming it's the model or the code.

**Operational note — Ollama's `keep_alive`**: during Part 4 testing, LLM first-token jumped back up to 7.5s (from the 587ms warm baseline) after roughly 10+ idle minutes spent on large downloads. Ollama unloads a model from VRAM after its default `keep_alive` window (5 minutes) with no requests. This means "pre-warm at startup" (Part 7) isn't a one-time fix — a demo with a long pause between turns, or the CLI sitting idle before a recording starts, could still hit a cold-start. Still unaddressed as of Part 6 — `FallbackManager` masks a slow *individual* turn with a filler phrase, but doesn't prevent or mitigate a cold-model turn from happening in the first place, and an actual cold start (~36s) is far beyond what a filler phrase can gracefully cover. Worth considering an explicit `keep_alive` value in the Ollama call options, or a periodic background ping, in Part 7.

## Does It Hit the 2-Second Budget?

On track but tighter than before, warm: STT (68ms) + retrieve_memory (25ms) + input guardrail (328ms) + LLM first token (587ms) + first-sentence TTS (~120ms) ≈ **1128ms** of the 2000ms budget (56%) to first audio out, leaving ~870ms for VAD + pipeline overhead + audio I/O latency (Part 7) — and that's *before* accounting for the output guardrail's per-sentence cost on sentences after the first (see Analysis above). extract_memory still doesn't count against this since it's fire-and-forget. The two "silent CPU fallback" bugs from Parts 1/4 are fixed, but the Ollama `keep_alive` finding means cold-start risk isn't fully closed out until Part 7 explicitly handles it, and guardrails have turned "comfortably inside budget" into "on track if nothing else adds up" — worth prioritizing the overlap mitigation (see Analysis) early in Part 7 rather than discovering it in end-to-end testing. The fallback threshold itself (800ms) also needs a second look once guardrails are actually in the loop — see the fallback-vs-guardrail note above, since the two Parts interact in a way that wasn't obvious until both were benchmarked.

## Implementation: Part 1 (STT), Part 2 (LLM first token, warm/cold), Part 3 (retrieve_memory latency), Part 4 (TTS per-sentence latency), Part 5 (guardrail check latency), and Part 6 (fallback filler-phrase overhead) done 2026-07-14/15. VAD numeric benchmark and full end-to-end (Part 7) pipeline still pending.
