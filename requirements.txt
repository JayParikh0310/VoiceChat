# === Audio I/O ===
pyaudio>=0.2.14              # mic input + speaker output
faster-whisper>=1.0.3        # STT (CTranslate2 backend, offline)
# silero-vad is loaded via torch hub

# === Speech / TTS ===
kokoro>=0.9.4                # Kokoro-82M TTS (preferred voice quality)
# piper-tts                  # Piper TTS (fallback, faster first chunk)

# === LLM + Orchestration ===
ollama>=0.3.0                # Ollama Python client (LLM served locally)
langgraph>=0.2.0             # Agent graph orchestration
langchain>=0.3.0             # LangChain core (used by LangGraph)
langchain-community>=0.3.0   # LangChain community integrations

# === Memory ===
faiss-cpu>=1.8.0             # Vector similarity search (offline)
sentence-transformers>=3.0.0 # all-MiniLM-L6-v2 embeddings (offline)

# === Guardrails ===
nemoguardrails>=0.10.0       # NeMo Guardrails (input + output rails only)

# === Web Demo (optional) ===
fastapi>=0.115.0             # FastAPI backend
uvicorn[standard]>=0.32.0    # ASGI server
websockets>=13.0             # WebSocket support for streaming audio

# === Dev / Testing ===
pytest>=8.0.0
pytest-asyncio>=0.24.0
python-dotenv>=1.0.0
pyyaml>=6.0.2
numpy>=1.26.0
torch>=2.3.0                 # Required by Silero VAD and sentence-transformers
torchaudio>=2.3.0            # Audio processing utilities
